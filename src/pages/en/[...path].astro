---
export const prerender = false;

/** Legacy /en/* → unprefixed EN canonical (default locale). */
const rest = Astro.params.path;
const target = rest ? `/${rest}/` : "/";
return Astro.redirect(target, 308);
---
